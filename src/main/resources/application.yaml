spring:
  application:
    name: spring-db-oracle
  profiles:
    active: database
  jpa:
#    hibernate:
#      ddl-auto: true
    show-sql: true
    properties:
      hibernate:
        format-sql: true
        dialect: org.hibernate.dialect.OracleDialect

server:
  port: 8080
  context-path: "/database"